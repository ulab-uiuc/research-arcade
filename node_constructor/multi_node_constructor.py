# TODO: extract information from link, figure and table

import json


class NodeConstructor:

    pass


    def construct_reference_node(self, reference: json):
        """
        It is hard to say since we cannot take only the abstract as information of a node
        It is highly likely that the 
        Reference: the json format reference of a paper
        """